# /about and /feedback are unlisted: reachable by link, kept out of search
# results. Disallow stops crawlers fetching them; each page also sends a
# noindex meta for any crawler that reaches it anyway.
User-agent: *
Disallow: /about
Disallow: /feedback
